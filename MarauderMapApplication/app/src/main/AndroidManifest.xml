<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hearc.ch.maraudermapapplication" >

    <uses-permission android:name="android.permission.INTERNET" >
    </uses-permission>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" >
    </uses-permission>
    <uses-permission android:name="android.permission.BLUETOOTH" >
    </uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".viewmap.ShowAllMapActivity"
            android:label="@string/title_activity_show_all_map" >
        </activity>
        <activity
            android:name=".viewmap.VisualisationPlanActivity"
            android:label="@string/title_activity_visualisation_plan" >
        </activity>
        <activity
            android:name=".createmap.CreateMapActivity"
            android:label="@string/title_activity_create_map" >
        </activity>
        <activity
            android:name=".createmap.BeaconConfigurationActivity"
            android:label="@string/title_activity_beacon_configuration" >
        </activity>
        <activity
            android:name=".settings.SettingsActivity"
            android:label="@string/title_activity_settings" >
        </activity>
    </application>

</manifest>
